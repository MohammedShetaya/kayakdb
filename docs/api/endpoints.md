if it gets a request on 


/get
it decodes the data as a key of one of (binary, string, number) types and searches for the value of that key 


/put
it decodes the data as key value

/put/many
decodes the data as a slice of key value 

